gitThis is the github project folder for ChenLiu, for practice and training use.

It's include personnal information.

git init

git add .
git commit -m "message"
git push origin master --force

git pull origin master